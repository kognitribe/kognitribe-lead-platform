import React from "react";
import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";
export default function Register(){const [form,setForm]=useState({name:"",email:"",password:"",confirmPassword:""}),[error,setError]=useState("");const {register}=useAuth();const nav=useNavigate();
 const submit=async e=>{e.preventDefault();setError("");if(form.password!==form.confirmPassword)return setError("Passwords do not match");try{await register(form);nav("/dashboard")}catch(err){setError(err.response?.data?.message||"Registration failed")}};
 return <div className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md glass rounded-3xl p-8"><h1 className="text-2xl font-semibold">Create your workspace</h1><p className="text-sm text-slate-500 mt-1">Start with responsible AI-assisted lead discovery.</p>{error&&<div className="mt-4 p-3 rounded-xl bg-red-500/10 text-red-300 text-sm">{error}</div>}{[["name","Name","text"],["email","Email","email"],["password","Password","password"],["confirmPassword","Confirm Password","password"]].map(([k,l,t])=><label className="block mt-4 text-sm" key={k}>{l}<input required type={t} minLength={k.includes("password")?8:undefined} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-2 w-full bg-black/20 border border-white/10 rounded-xl px-3 py-3"/></label>)}<button className="mt-6 w-full py-3 rounded-xl bg-accent font-semibold">Create Account</button><p className="mt-5 text-center text-sm text-slate-500">Already have an account? <Link className="text-accent" to="/login">Login</Link></p></form></div>
}
