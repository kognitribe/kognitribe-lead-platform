import React from "react";
import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";import {Sparkles} from "lucide-react";
export default function Login(){const [form,setForm]=useState({email:"",password:""}),[error,setError]=useState("");const {login}=useAuth();const nav=useNavigate();
 const submit=async e=>{e.preventDefault();setError("");try{await login(form);nav("/dashboard")}catch(err){setError(err.response?.data?.message||"Login failed")}};
 return <div className="min-h-screen grid lg:grid-cols-2"><div className="hidden lg:flex flex-col justify-center p-16 bg-gradient-to-br from-accent/20 to-cyan/5"><Sparkles className="text-accent"/><h1 className="text-5xl font-semibold mt-8">Turn public signals into qualified opportunities.</h1><p className="mt-5 text-slate-400 max-w-lg">Kognitribe's lead intelligence workspace brings discovery, AI analysis and responsible outreach together.</p></div><div className="grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md glass rounded-3xl p-8"><div className="text-2xl font-semibold">Welcome back</div><p className="text-sm text-slate-500 mt-1">Sign in to your lead workspace.</p>{error&&<div className="mt-4 p-3 rounded-xl bg-red-500/10 text-red-300 text-sm">{error}</div>}<label className="block mt-7 text-sm">Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="mt-2 w-full bg-black/20 border border-white/10 rounded-xl px-3 py-3 outline-none focus:border-accent"/></label><label className="block mt-4 text-sm">Password<input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="mt-2 w-full bg-black/20 border border-white/10 rounded-xl px-3 py-3 outline-none focus:border-accent"/></label><button className="mt-6 w-full py-3 rounded-xl bg-accent font-semibold">Login</button><div className="mt-5 flex justify-between text-sm"><Link className="text-accent" to="/forgot-password">Forgot password?</Link><Link className="text-slate-400" to="/register">Create account</Link></div><Link to="/" className="block mt-6 text-xs text-center text-slate-600">← Back to landing</Link></form></div></div>
}
